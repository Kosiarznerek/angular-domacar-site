import {ShopCategoryToStringPipe} from './shop-category-to-string.pipe';

describe('ShopCategoryToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopCategoryToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
